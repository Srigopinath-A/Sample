---
- name: To install mail x
  hosts: localhost
  become: yes
  become_method: sudo
  tasks:

    - name: to install mailx
      ansible.builtin.apt:
        name: bsd-mailx
        state: present
      when: ansible_os_family == 'Debian'
