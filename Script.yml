---
- name: the mail have to send 
  hosts: localhost
  become: fales
  become_user: root
  tasks:
    - name: Script call
      ansible.builtin.command:
        cmd: python3 Sendmain.py
      args:
        chdir: /home/azureuser
